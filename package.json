{
  "devDependencies": {
    "playwright": "^1.55.0",
    "web-ext": "^10.6.0"
  },
  "name": "snowmobile",
  "description": "Make the snowheads.com forum mobile friendly.",
  "version": "1.3.0",
  "main": "index.js",
  "dependencies": {},
  "scripts": {
    "screenshot": "node scripts/capture-screenshot.js",
    "screenshot:iphone17e": "VIEWPORT_WIDTH=390 VIEWPORT_HEIGHT=844 node scripts/capture-screenshot.js",
    "lint": "web-ext lint --ignore-files AGENTS.md --ignore-files README.md --ignore-files package.json --ignore-files yarn.lock --ignore-files playwright.config.js --ignore-files 'scripts/**' --ignore-files 'screenshots/**'",
    "release": "web-ext build --overwrite-dest --ignore-files AGENTS.md --ignore-files README.md --ignore-files package.json --ignore-files yarn.lock --ignore-files playwright.config.js --ignore-files 'scripts/**' --ignore-files 'screenshots/**'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oferm/snowmobile-webextension.git"
  },
  "author": "scr",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/oferm/snowmobile-webextension/issues"
  },
  "homepage": "https://github.com/oferm/snowmobile-webextension#readme"
}
